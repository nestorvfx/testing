{
  "name": "oci-speech-server",
  "version": "1.0.0",
  "description": "OCI Speech Authentication Server",
  "main": "index.js",  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --exec ts-node index.ts",
    "build": "tsc",
    "build:prod": "npm run build && npm prune --production",
    "start:prod": "NODE_ENV=production npm start",
    "test": "echo \"No tests specified\" && exit 0",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/dotenv": "^6.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "oci-aispeech": "^2.68.0",
    "oci-common": "^2.68.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
