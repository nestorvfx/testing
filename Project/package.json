{
  "name": "perplexityscenecapture",
  "version": "1.0.0",
  "description": "Perplexity Scene Capture with OCI Speech Integration",
  "scripts": {
    "setup": "node setup.js",
    "start:client": "cd client && expo start",
    "start:server": "cd server && npm run dev",
    "start:both": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:client": "cd client && expo build:web",
    "build:server": "cd server && npm run build",
    "build:all": "npm run build:server && npm run build:client"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "private": true,
  "workspaces": [
    "client",
    "server"
  ]
}
